import { Container } from '@UI/layout';
import Token from '@Components/token/Token';

const TokenPage = () => {
  return (
    <Container>
      <Token />
    </Container>
  );
};

export default TokenPage;
