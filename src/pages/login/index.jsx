import { Container } from '@UI/layout';
import Login from '@Components/login/Login';

const LoginPage = () => {
  return (
    <Container>
      <Login />
    </Container>
  );
};

export default LoginPage;
